{
  "name": "Serial Messenger",
  "description": "Communicate with a serial port via message events",
  "version": "0.0.1",
  "offline_enabled": true,
  "app": {
    "background": {
      "scripts": ["background.js"]
    }
  }
}
